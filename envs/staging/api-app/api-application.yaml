apiVersion: apps/v1
kind: Deployment
metadata:
  name: food-macro-api
  namespace: staging-essi
  labels:
    app: food-macro-api
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: food-macro-api
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: food-macro-api
    spec:
      containers:
        - name: food-macro-api
          image: ezbaa/food-macro-api:latest
          imagePullPolicy: Always
          resources: {}
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: GROQ_API_KEY
              valueFrom:
                secretKeyRef:
                  name: groq-secret
                  key: GROQ_API_KEY
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30